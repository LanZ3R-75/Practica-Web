import CrearContenido from '@/components/comercios/crearContenido';

export default function CrearContenidoPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between">
      <CrearContenido />
    </main>
  );
}